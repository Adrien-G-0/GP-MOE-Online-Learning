Spécifications du Contenu des Fichiers Source (src/)L'agent doit implémenter les fichiers suivants en respectant ces responsabilités strictes :src/data_stream.py :Créer une classe ou un générateur qui prend un jeu de données complet et simule l'arrivée séquentielle des observations $(x_i, y_i)$ une par une.Inclure une fonction pour standardiser les données (moyenne zéro, variance unitaire).src/kernel.py :Implémenter la fonction du noyau RBF (Radial Basis Function).Inclure le calcul de la matrice de covariance avec ajout du bruit $\sigma^2 I$ sur la diagonale.src/gp_expert.py :Créer une classe GPExpert représentant un cluster individuel.Stocker les observations affectées à ce cluster.Implémenter le calcul de la vraisemblance marginale (nécessaire pour la mise à jour des poids SMC).Implémenter l'Elliptical Slice Sampler (ESS) pour la mise à jour des hyperparamètres (Éviter Metropolis-Hastings classique).Optionnel : Implémenter la logique d'approximation stochastique (minibatching) si le nombre d'observations $N_k$ dépasse la limite $B$.src/crp.py :Créer les fonctions pour calculer la probabilité a priori d'assignation d'une nouvelle observation à un cluster existant ou à un nouveau cluster, en utilisant la distribution T-multivariée marginalisée.src/smc_sampler.py :Gérer un ensemble de $J$ particules. Chaque particule contient un état (assignations $z$, hyperparamètres $\theta$, concentration $\alpha$).Implémenter la fonction de calcul des poids $w_i^{(j)}$ via le ratio de vraisemblance.Implémenter le calcul de $N_{eff}$ et la fonction de rééchantillonnage multinomial pour pallier la dégénérescence des particules.src/gp_moe.py :Exposer l'API principale de haut niveau : update(x_i, y_i) et predict(x_test).Orchestrer l'appel au smc_sampler à chaque nouvelle donnée et moyenner les prédictions (Algorithme 2 du papier).